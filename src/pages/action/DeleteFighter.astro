---
import Fighter from "../../components/Fighter.astro";
import Layout from "../../layouts/Layout.astro";
import { fighterDatas } from "../../lib/fighterDatas";
import { showCaseData } from "../../lib/showCaseData";
import Delete from "../../components/Delete.astro";
---

<Layout title="FighterCard">
  <div class="showCase">
    {showCaseData.map((showCase) => <Delete showCase={showCase} />)}
  </div>
  <!-- 
  <div class="showCase">
    {showCaseData.map((showCase) => <ShowCase showCase={showCase} />)}
  </div>
-->

  <div class="container">
    {fighterDatas.map((fighterData) => <Fighter fighterData={fighterData} />)}
  </div>
</Layout>

<style>
  body {
  }
  .showCase {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 70px;
    padding: 50px;
    max-width: 1150px;
    width: 100%;
    height: auto;
    margin: 0 auto;
  }
  .container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 70px;
    padding: 30px;
    max-width: 1150px;
    width: 100%;
    height: auto;
    margin: 0 auto;
  }
</style>
