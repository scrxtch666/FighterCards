---
interface Props {
  maxWidth?: string;
}
const { maxWidth = "max-w-screen-xl" } = Astro.props;
---

<div class="max-w-xl mx-auto w-full px-5 flex flex-col gap-5 min-h-dvh overflow-hidden">
  <slot />
</div>
